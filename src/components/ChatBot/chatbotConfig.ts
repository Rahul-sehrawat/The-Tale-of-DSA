import { createChatBotMessage } from 'react-chatbot-kit';

const config = {
    botName :'Rahul\'s chatbot',
  initialMessages: [createChatBotMessage("Hi! I'm here to help you about dsa questions.",{})],
};

export default config;
